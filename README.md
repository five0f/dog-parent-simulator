# Dog parent simulator

Browser game prototype about raising Bublik, a corgi living an ordinary life with an ordinary
person.

## Commands

- `npm run dev`
- `npm run build`
- `npm run check`
- `npm run check:fix`
